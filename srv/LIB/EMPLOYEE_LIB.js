const cds = require("@sap/cds");
// var alasql = require('alasql');

exports.fetchEmployeeId = async function () {
    // let connection = await cds.connect.to ('db');
    // // console.log(connection.entities['EMPLOYEE']);
    // let queryResult =  await connection.run(SELECT .from(`${connection.entities['EMPLOYEE']}`, employee =>{employee.EMPLOYEE_ID}) .orderBy('EMPLOYEE_ID desc') .limit(1) );
    // // let queryResult =   await connection.run(SELECT.from `${connection.entities['EMPLOYEE']}` .columns `{EMPLOYEE_ID}`)
    // // console.log(queryResult[0].EMPLOYEE_ID - '0' );
    // let employeeIDResult = 1;
    // employeeIDResult += queryResult[0].EMPLOYEE_ID - '0';
    // // console.log(employeeIDResult);
    // return employeeIDResult;
 };